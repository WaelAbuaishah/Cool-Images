package nabed.apps.services.repository


class UserRepositoryTest {
//    @MockK
//    lateinit var userRepository: UserRepository

//    @Before
//    fun setUp() {
//        MockKAnnotations.init(this) //for initialization
//    }

   /* @Test
    fun login()  {
        val loginParams = LoginParams(
            userName = "dr.rami",
            password = "123456"
        )
        every {
            runBlocking {
                val user = userRepository.loginUser(loginParams)
                MatcherAssert.assertThat(
                    user.status.name,
                    CoreMatchers.`is`(Status.SUCCESS.name)
                     )
            }
        }

    }*/

}